spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgresFinal
    username: postgres
    password: 1234
    hikari:
      maximum-pool-size: 5
  mvc:
    hiddenmethod:
      filter:
        enabled: true